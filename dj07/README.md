# Django + Telegram Bot Integration

## Описание проекта
Проект демонстрирует интеграцию Django REST API с Telegram-ботом. 
Реализована клиент-серверная архитектура:
- **Сервер**: Django REST API (обработка данных, бизнес-логика)
- **Клиент**: Telegram-бот (взаимодействие с пользователями)

## Цели проекта
1. Создание Telegram-бота с использованием библиотеки `telebot`
2. Настройка бота для отправки запросов в Django API
3. Регистрация пользователей через команду `/start`
4. Получение информации о пользователях через API
5. Обработка POST-запросов от бота и отправка JSON-ответов

## 🏗️ Архитектура проекта
Пользователь ↔ Telegram Bot ↔ Django REST API ↔ База данных
(клиент) (сервер)

text

## 📁 Структура проекта
project_dj07/
├── djangobot/ # Основной проект Django
│ ├── init.py
│ ├── settings.py # Настройки проекта
│ ├── urls.py # URL маршруты
│ ├── asgi.py
│ └── wsgi.py
├── bot/ # Приложение для работы с API
│ ├── init.py
│ ├── models.py # Модель TelegramUser
│ ├── views.py # API представления
│ ├── serializers.py # Сериализаторы
│ └── urls.py # API маршруты
├── bot_main.py # Telegram бот
├── test_api.py # Скрипт тестирования API
├── manage.py # Django управление
├── requirements.txt # Зависимости
└── README.md # Документация

text

## 🚀 Установка и запуск

### 1. Клонирование и настройка
bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

## Запуск проекта
1. Откройте браузер
2. Перейдите по адресам:
   - http://127.0.0.1:8000/ (главная страница с информацией об API)
   - http://127.0.0.1:8000/api/users/ (список пользователей)
   - http://127.0.0.1:8000/admin/ (админка, если создан суперпользователь)